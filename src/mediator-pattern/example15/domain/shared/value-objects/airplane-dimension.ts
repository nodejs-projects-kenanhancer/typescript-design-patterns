export class AirplaneDimension {
  length: number; // in meters
  wingspan: number; // in meters
  height: number; // in meters

  constructor(length: number, wingspan: number, height: number) {
    this.length = length;
    this.wingspan = wingspan;
    this.height = height;
  }
}
